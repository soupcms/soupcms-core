javascript:
  asyncLoader(['//code.jquery.com/jquery-2.1.1.min.js']);

- page.javascripts.each do |js|
  javascript:
    asyncLoader(['#{javascript_tag(js)}']);

javascript:
  WebFontConfig = {
    google: {families: ['Open+Sans:400,300,600,700']}
  };
  asyncLoader(['//ajax.googleapis.com/ajax/libs/webfont/1.4.7/webfont.js']);

javascript:
  asyncLoader(['//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css']);
